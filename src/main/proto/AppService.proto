syntax = "proto3";

package info.vziks.grpc;

message AppRequest{
  string name = 1;
  repeated string arr = 2;
}

message AppResponse {
  string hello = 1;
  repeated string arr = 2;
}

service AppService {
  rpc hello(AppRequest) returns (AppResponse);
}